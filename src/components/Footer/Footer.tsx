import React from 'react';
import StyledFooter from './StyledFooter';

const Footer = () => {
    return (
        <StyledFooter>
            <div className='inner'>

            </div>
        </StyledFooter>
    );
};

export default Footer;